---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: duplicators.syoze.syoze.fr
spec:
  group: syoze.syoze.fr
  names:
    kind: Duplicator
    listKind: DuplicatorList
    plural: duplicators
    singular: duplicator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Duplicator is the Schema for the duplicators API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DuplicatorSpec defines the desired state of Duplicator.
            properties:
              namespaceSelector:
                description: NamespaceSelector selects the namespaces to duplicate
                  resources from.
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              targetResources:
                description: TargetResources specifies the resources to duplicate.
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  type: object
                type: array
            type: object
          status:
            description: DuplicatorStatus defines the observed state of Duplicator.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
